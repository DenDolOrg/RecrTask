@model IEnumerable<RekrutTask.Models.Person>

@{
    ViewBag.Title = "Person List";
}

@if (Model.Count() > 0)
{
    foreach (var p in Model)
    {
        Html.RenderPartial("MainPersonInfo", p);
    }
}
else
{
    <h3>List is empty</h3>
}
